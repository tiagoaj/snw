# SNW Frontend

Projeto React + Vite para o painel SNW.

## Execução

1. Abra o diretório `frontend`
2. Execute `npm install`
3. Execute `npm run dev`

## Endpoints usados

- `POST /api/auth/login`
- `GET /api/workspaces/me`
- `GET /api/clients?workspace_id=...`
